# Project Rules: mockup_generator

## 代码规模与单文件行数限制（强制规则）

### 1. 1,000 行红线与主动预警
- **触发条件**：项目内任何源文件（包括但不限于 `.jsx`、`.js`、`.css`）代码行数**超过 1,000 行**。
- **强制行为**：AI 助手在读取、分析、创建或修改该文件时，**必须立即主动提醒用户**，指出超限文件、当前行数及潜在的维护与膨胀风险。

### 2. 主动重构分析与瘦身方案
当检测到超 1,000 行代码时，AI 必须主动为用户提供以下三个维度的分析与拆分规划：
1. **代码瘦身（Code Slimming）**：
   - 检查并清理未引用的 import、死代码（dead code）以及重复冗余的样板代码。
2. **逻辑/组件拆分（Component Modularization）**：
   - **JSX 组件**：依据单一职责原则（SRP），将巨型单文件拆分为功能子组件（如 Tab 页、表单区域、弹窗、工具条、自定义 Hooks 等），存入同级子目录或 `components/` 模块中；
   - **状态解耦**：通过 Context（如 `useLcs`）或 Props 明确依赖，降低单文件状态复杂度。
3. **样式分层与解耦（CSS Splitting）**：
   - **CSS 样式**：按业务模块或 DOM 区域细分为子文件（如 `viewport.css`、`channels.css`、`modals.css` 等），单文件保持在 500-800 行以内；
   - 通过二级子目录与聚合 `@import` 统一引入，保持对外引用路径稳定。

### 3. 防劣化门禁
- 新增功能或日常开发维护时，严禁在已接近或超过 1,000 行的文件中盲目堆砌代码。必须先拆分瘦身，再进行业务迭代。
